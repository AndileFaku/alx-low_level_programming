#include <stdio.h>
/**
 * main - Entry point for single digit numbers of base 10
 * Return: 0 (success)
 */

int main()
{
	int a;

	for(a = 0; a < 10; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}
